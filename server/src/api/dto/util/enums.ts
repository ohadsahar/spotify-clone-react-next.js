export enum ErrosType {
	BADREQUEST = 'Bad Request',
	FORBIDDEN = 'Forbidden'
}

export enum ESocketType {
	connection = 'connection',
	live = 'live',
	join = 'join',
	leave = 'leave',
	user_report = 'user_report',
	out_for_commercials = 'out_for_commercials',
}
